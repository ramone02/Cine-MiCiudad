import { BrowserRouter, Route, Routes } from "react-router-dom";

export const Navigation = () => {
  return (
    <BrowserRouter>
      <div></div>;
      <Routes>
        <Route />
      </Routes>
    </BrowserRouter>
  );
};
